<div id="<%= dom_id category %>" class="d-inline-block m-2">
  <div class="card p-2">
  <%= turbo_frame_tag "cat_edit_#{category.id}" do %>
    <%= category.name %>
    <%= link_to 'edit', edit_category_path(category.id) %>
  <% end %>
  </div>
</div>