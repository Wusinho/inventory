<div id="<%= dom_id f.object %>" class='nested-form-wrapper d-inline-block my-1' data-new-record="<%= f.object.new_record? %>" >
  <div class="<%= payment_status(f) %>" >
    <div class="card-body p-1">
      <p class="card-text"><%= f.object.customer.name %></p>
      <p class="card-text">cant: <%= f.object.quantity %></p>
    </div>
  </div>
</div>