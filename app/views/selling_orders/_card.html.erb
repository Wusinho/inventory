<div id="<%= dom_id f.object %>" class='nested-form-wrapper d-inline-block' data-new-record="<%= f.object.new_record? %>" >
  <div class="<%= payment_status(f) %>" >
    <div class="card-body">
      <p class="card-text">name: <%= f.object.customer.name %></p>
      <p class="card-text">quantity: <%= f.object.quantity %></p>
    </div>
  </div>
</div>